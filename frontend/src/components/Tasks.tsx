const Database = () => {
  const manageTasks = () => {};
  return (
    <div>
      <div className="subsection--title">
        <h2>Tasks</h2>
        <button onClick={manageTasks}>Manage tasks</button>
      </div>
      tasks including defaults like reading or custom like drawing-to-cad
    </div>
  );
};

export default Database;
